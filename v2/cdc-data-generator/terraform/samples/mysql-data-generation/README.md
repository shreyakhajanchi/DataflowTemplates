## Sample Scenario: MySQL Data Generation Job Launch

This Terraform example illustrates launching a data generation job targeting a MySQL sink, setting up the required auxiliary staging GCS bucket, and executing the CDC Data Generator Flex Template.

## Description

This sample contains the following files:

1. `main.tf` - Contains the Terraform resources to be created (GCS bucket, local sink config and schema overrides uploads, IAM permissions, and the Dataflow Flex Template Job).
2. `outputs.tf` - Declares the output values generated by the module (such as resource IDs and direct Google Cloud Console URLs).
3. `variables.tf` - Declares the structured input variables required to configure the resources.
4. `terraform.tf` - Declares the required providers and GCP API configurations.
5. `terraform.tfvars` - Contains the default/sample configurations matching your custom project networks and targets to help you quickly get started.
6. `mysql-sink-options.json` - A self-contained sample local configuration file illustrating host, port, credentials, and shard layout representation.

## Resources Created

This module provisions the following resources:

1. **GCS Bucket** - A bucket to hold staged configurations (sink options, schema overrides) and the default Dead-Letter Queue (DLQ) directory.
2. **Dataflow Flex Template Job** - Launches the `Cdc_Data_Generator` pipeline with `sinkType = "MYSQL"`.
3. **Permissions** - Automatically assigns the required roles (`roles/storage.objectAdmin`, `roles/dataflow.worker`, etc.) to your Dataflow Runner Service Account if `add_policies_to_service_account` is set to `true`.

## How to Run

1. Clone or copy the sample folder locally.
2. Edit the `terraform.tfvars` file and replace the values with your own project, network, and target options GCS file path.
3. Run the following standard commands:

### Initialize Terraform
```shell
terraform init
```

### Plan and Apply
Validate the resources to be created:
```shell
terraform plan
```

Deploy the infrastructure and launch the Dataflow job:
```shell
terraform apply
```

Upon completion, Terraform will print direct Google Cloud Console resource URLs to monitor the running Dataflow Job.

### Cleanup
To stop the running generator job and tear down the created staging bucket, run:
```shell
terraform destroy
```
